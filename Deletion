#include <stdio.h>
int main()
{
int a[100],n=7;
int i,pos;
printf("Enter the elements of an array:");
for(i=0;i<n;i++)
scanf("%d",&a[i]);
printf("Enter the position of element to be deleted :");
scanf("%d",&pos);
for(i=pos;i<n;i++)
a[i-1]=a[i];
for(i=0;i<n-1;i++)
printf("%d",a[i]);
return 0;
}
